## Task 7 report

**Status:** done.

**Commit:**
- `d280f86` feat: E-STATS-RESAMPLE-UNITS, so retiring the refusal cannot open a silent no-op

**Tests:** `uv run pytest` — 1721 passed, 2 xfailed (baseline 1719 + 2 xfailed; 2 new tests added). `uv run ruff check .` clean. `uv run mypy` clean. Mutation (`if not units_declared:` → `if roster is None:`) confirmed `test_an_unresolvable_roster_does_not_earn_a_second_resample_finding` FAIL while `test_a_resample_with_no_unit_roster_is_refused` stayed PASS, then reverted in place (`__pycache__` cleared between).

**Judgment call the brief flagged as open:** gate on `not units_declared` (i.e. `(doc.get("data") or {}).get("units")` truthy), not on `roster is None`. Confirmed empirically: with `data.units: {from: nope.csv, key: patient_id}` (a real file that doesn't exist), `roster` is `None` but `units_declared` is truthy, so gating on `roster is None` would fire `E-STATS-RESAMPLE-UNITS` on top of `E-UNITS-SOURCE-MISSING` — the exact double-fault the brief's `usable_cluster` precedent warns against. Gating on the declaration reports only the pre-existing `_check_units` finding in that case, and still refuses the true no-roster shape (no `data.units` block at all). Both tests distinguish the two cases as required — one config with no `data.units` key at all, the other with `data.units` declared but pointing at a nonexistent file.

**No disagreement with the brief found this time** — the six-implementers framing in my instructions expected one, but the brief's own "Gate on the declaration, not on `roster is None`" instruction matched what the code needed exactly, and the precedent (`_check_replication`'s fold-without-basis shape) was a clean fit with no adaptation required. I looked for a mismatch specifically (re-read `_check_resample`'s existing four checks, `_check_replication`'s `E-REPL-FOLD-K` site, and the `usable_cluster` guard in `validate_config`) and found none worth reporting.

**Interface delivered, unchanged from the brief:** the new check sits at the top of `_check_resample`, right after the `not isinstance(resample, dict) or not resample` guard, and returns before any of the four existing checks (method enum, `n` floor, family bound, strata names) — none of which read `units_declared`, so the ordering only affects whether they run at all when there's no roster to check. `E-STATS-RESAMPLE-UNSUPPORTED` still fires wholesale from `_check_unimplemented` regardless (verified: the "no roster" test's positive-companion config still carries `-UNSUPPORTED` alongside the new code, since neither test asserts uniqueness of findings). Registered in `docs/reference.md` § Errors `validate` reports (alphabetically last among the `E-STATS-RESAMPLE-*` rows, after `-STRATIFY-UNKNOWN`) and § Validation (new row *Resample has a roster*, placed immediately before *Clusters enough to resample* since it's the precondition that check and the `n`-floor/family-bound checks all presuppose). No count-phrase near either table needed updating — grepped both tables' surrounding prose for row-count phrasing and found none, matching task 4's prior finding.

**Concerns:** none. The two new tests are the fixture-distinguishing pair the brief asked for, and the mutation confirmed they actually distinguish the two cases rather than both passing under either gate.

---

**Coordinator review response (commits `d280f86`..HEAD, review `01b2b97`): spec ✅, quality approved with findings — 2 Important, 3 Minor. Addressed all five.**

1. **Important — the comment miscited its precedent.** It named `_check_replication`'s `E-REPL-FOLD-K`; the real twin is `E-REPL-FOLD-NO-UNITS`, the same `not (doc.get("data") or {}).get("units")` expression twelve lines away, silent for the same unresolvable-roster reason. `E-REPL-FOLD-K` is the unrelated `k: all`-basis-unknowable fault. The miscitation originated in the spec's trap table, not in this task's own reading — but the standing obligation is to audit an inherited comment against the code regardless of where it came from, and that audit should have caught it before commit. Fixed the comment to cite `E-REPL-FOLD-NO-UNITS` by name and note what `E-REPL-FOLD-K` actually is. The corrected precedent is *stronger* than the miscited one: it makes the declaration-vs-`roster is None` gate an established house pattern rather than a novel call.

2. **Important — the comment claimed a false guarantee, and the code matched the false claim.** It said every check below the gate "presupposes a roster it would have nothing to read." False: `roster` is an unused parameter across the whole function, and `method`/`n` are read from `resample` itself. The `return` I had written therefore suppressed two independently-fixable faults — `{method: bootstap, n: 50}` with no `data.units` reported only `E-STATS-RESAMPLE-UNITS`, silently dropping `E-STATS-RESAMPLE-METHOD` and `E-STATS-RESAMPLE-N` — matching `_check_replication`'s twin, which reports and does not `return`. Fixed by dropping the `return`, following the twin. Added a new test, `test_a_missing_roster_does_not_swallow_the_roster_independent_resample_faults`, asserting all three codes fire together for `{method: bootstap, n: 50}` with no `data.units`; mutation (re-inserting the `return`) confirmed it FAILS, reverted in place.

3. **Minor — the positive companion asserted only an absence.** `assert "E-STATS-RESAMPLE-UNITS" not in codes(...)` would stay green if the companion config were silently broken (e.g. `data.units: 5`, which dies upstream at fatal `E-CONFIG-SHAPE` before this check ever runs). Changed to pin the whole set: `codes(...) == {"E-STATS-RESAMPLE-UNSUPPORTED"}`. Mutation (renaming the field `_check_unimplemented` reads from `"resample"` to `"resample_MUTATED"`, so `-UNSUPPORTED` stops firing) confirmed FAIL, reverted in place.

4. **Minor — the positive half of the unresolvable-roster test used a broad prefix.** `any(code.startswith("E-UNITS-") or code.startswith("E-DATA-"))` passes on any code in a large family, not necessarily the actionable one. Named the exact code, `E-UNITS-SOURCE-MISSING`. Mutation (changing the broken config from a missing file to a missing key, so `E-UNITS-KEY-MISSING` fires instead) confirmed FAIL, reverted in place.

5. **Minor — this report inherited the false roster claim and carried process noise.** The "Interface delivered" paragraph said the ordering "only affects whether they run at all when there's no roster to check" — same falsehood as the comment. Corrected above: dropping the `return` means the ordering only affects finding order, not whether the later checks run. Removed the "No disagreement… six-implementers framing" paragraph — that referred to a prompt instruction a future reader of this tracked file will not have, and reads as noise once the actual finding (a miscited, over-claiming comment) is on record.

**Recorded for its owner:** `statistics.null_test` has no equivalent no-units check — whichever slice retires `E-STATS-NULLTEST-UNSUPPORTED` (H4d) inherits this exact hole. Logged in `docs/superpowers/spec-defects.md` under "`statistics.null_test` has no no-units check, unlike `fold` and `resample`", naming the proposed code (`E-STATS-NULLTEST-UNITS`) and the same report-without-return shape.

**Final:** `uv run pytest` 1722 passed + 2 xfailed (1721 + 1 new guarding test). `ruff check .` clean. `mypy` clean. All five changed/added assertions mutation-tested individually — apply, confirm FAIL, clear `__pycache__`, revert in place (never `git checkout`), confirm PASS.
