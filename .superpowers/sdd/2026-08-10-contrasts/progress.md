# SDD ledger — plan: docs/superpowers/plans/2026-08-10-contrasts.md
Branch s4b-contrasts off main at 0fc2a6f (632 tests, ruff/mypy clean).
Pre-flight (mine): verified Interval still has exactly three fields (low/high/method) so nothing unpacks or indexes it; that _unit_table_from_rows and _percentile_ranks exist for Task 5 to reuse rather than re-derive; and that tests/test_sweep.py builds Condition(index=, label=, values={}, is_baseline=) exactly as Task 1's tests assume. Also fixed one sloppy assertion of my own — a run.get('results', run) hedge that would have passed without looking in the right place.
Pre-flight scope note: welch_t_over_units and unpaired_percentile_over_units are UNREACHABLE while allocation: between is refused, as are the _clustered variants while cluster_by is refused. The plan documents them as out of scope rather than building them.
Task 1: implemented at 1d24fb4 (contrasts.py: Comparison + resolve_contrasts, pure, Condition under TYPE_CHECKING). 637 tests. Verified: no baseline and none declared gives [], vs_baseline yields one per non-baseline condition against the baseline index, declared entries follow with their within stratum.
SESSION LIMIT: Task 1's reviewer and Task 2's implementer were both killed before producing anything — tree clean, no commits past 1d24fb4, no task-1-review.md written. Both re-dispatched.
Task 1: complete (0fc2a6f..1d24fb4, spec ✅, quality approved). Reviewer confirmed purity, ordering, the empty case, the KeyError comment, and that Comparison is correctly NOT exported from publishable/__init__.py — an internal record type, not user-facing surface.
Task 1: minor (deferred): entry.get('id') coerces a missing id to the string 'None' and the comment documents only the of/against KeyError path; within= passes the caller's dict by reference rather than copying, a small crack in the frozen-dataclass guarantee.
Task 1: reviewer notes the label=None branch is currently unreachable dead code — sweep.expand never makes a no-sweep single condition is_baseline=True, so that case already returns [] via the no-baseline branch. Correct behaviour, just not reachable.
Task 2: implemented at 203143a (units_matching; None vs set() preserved; conjunctive; values compared as strings). 642 tests. Verified all five behaviours by probe.
Task 2: complete (1d24fb4..203143a, spec ✅, quality approved). Reviewer confirmed both sides of the value comparison are coerced, the None/set() short-circuit, and that test_multiple_levels_are_conjunctive would catch a disjunctive implementation.
Task 3: implemented at c15f3c1 (paired_keys: sorted intersection, allowed narrowing, empty-allowed distinguishable from None). 647 tests, one test added beyond the brief's four.

Task 2: CARRY TO TASK 6 — an unknown attribute name in a within stratum is indistinguishable from a genuinely empty one, since attributes.get() returns None for a typo. The reviewer notes this belongs in validate, analogous to report_by's unknown-attribute rule near reference.md:2124, and is correctly out of scope for a pure function.
Task 3: complete (203143a..c15f3c1, spec ✅, quality approved). Reviewer confirmed the discriminating test pins all three wrong implementations and that the added empty-allowed test pins the None-vs-set() distinction rather than just the empty result.
Task 3: minor (deferred, from my brief): test_the_result_is_sorted relies on Python set iteration order being non-monotonic for two short strings, which is hash-seed dependent — the narrow sorted-to-unsorted-list mutation is not guaranteed caught.
Task 4: implemented at 23c830a (paired_t_over_units, cohens_dz). 653 tests. The hand-computed cohens_dz value 1.93649167 held exactly.
Task 4: complete (c15f3c1..23c830a, spec ✅, quality approved). Reviewer independently recomputed the constant (sample sd 1.2909944 -> dz 1.93649167; a ddof=0 swap would give 2.2360680, visibly different) and confirmed paired_t_over_units purely delegates so it cannot drift from t_over_units.
Task 5: implemented at ddf2769 (paired_percentile_of_derived, one draw both sides, reusing min_honest_draws and _percentile_ranks). 658 tests. Implementer wrote the independent-draw version first and confirmed the narrowness test fails against it.
Task 5: PLAN DEFECT (mine) — my brief's fixture used a CONSTANT per-unit offset, so the CORRECT paired implementation returns a zero-width interval at exactly 0.5, failing my own strict-bracket test and making the seed-7-vs-99 inequality unsatisfiable. The implementer alternated the offset so real resample variance exists, then re-verified the discrimination still holds. Verified myself: paired width 0.2500 against independent 17.5500, a 70x reduction.
Task 5: review spec ✅, quality findings (3 Important). (a) stats.py:271-275 the try wraps table construction AND the of[k]/against[k] lookups, where the unpaired sibling deliberately builds tables outside and wraps only compute — so a key missing from either side becomes 2000 swallowed KeyErrors returning (None, 0), indistinguishable from every-draw-degenerate. (b) no test uses a compute that raises, returns nan, or fails on only ONE side, so narrowing the try to side a would pass all five tests. (c) the discarded constant fixture was the SHARPER discriminator (paired width exactly 0.0 against ~12.4 independent) and should have been added to, not replaced.
Task 5: reviewer replayed the draw sequence and confirmed my brief's constant fixture gives exactly 0.5 at both ranks for seeds 7 and 99 — so zero-width is genuinely correct behaviour for a point-mass bootstrap, and MY two assertions were wrong rather than the data. Narrowness margin measured at 17.5x; catches independent draws, one-side-unresampled, and per-side RNG.
Task 6: implemented at 454aa4f + 70c6f5c (declared contrasts accepted; E-STATS-CONTRAST-NESTED, E-STATS-CONTRAST-UNKNOWN, E-STATS-CONTRAST-WITHIN minted; E-STATS-CONTRASTS-UNSUPPORTED retired). 663 tests. The implementer self-caught via advisor review that a name which is BOTH a real condition label and another entry's id was wrongly flagged nested — label now wins.
Task 6: complete (ddf2769..70c6f5c, spec ✅, quality approved). Reviewer confirmed nesting-before-unknown is real logic pinned by a test whose of: 'a' fails as a label too, that E-STATS-CONTRAST-WITHIN was grepped clean before minting, and that E-STATS-CONTRASTS-UNSUPPORTED is gone from src/, tests/ and the four documents. Label-wins judged the correct call.
Task 6: minor (deferred): no dedicated test pins the label/id collision — label-wins is proven only by docstring and report, not by an assertion.
Task 5: fix round 1 at a79829d (try narrowed to compute only; one-sided, raising and nan cases pinned; constant fixture restored under a zero-width assertion alongside the alternating one).
Task 5: complete (23c830a..a79829d, all three findings ADDRESSED, no new breakage).
Task 7: implemented at 5349b0e (vs_baseline at results.conditions[i].vs_baseline.<step>.<metric>, sibling to aggregated; W-STATS-CONTRAST-THIN minted for min_reported_n; run_a_project gains units=; a _METHOD_VARYING_STEP fixture added because the existing one records identical values across conditions and let delta/cohens_d pass degenerately). 672 tests.
Task 7: CRITICAL found by the implementer's own concern and confirmed by me — paired_percentile_of_derived takes ONE compute, and Task 7 passes the reporting condition's closure for both sides. In the documented worked example the step records pred/truth identically and only the FORMULA differs by condition, so both collapsed tables are the same: I measured ci95 [0.0000, 0.0000], zero width at zero, against a nonzero point estimate. An interval that does not contain its own point estimate. This is MY plan defect — Task 5's signature.
Task 7: CRITICAL, second — declared statistics.contrasts now validate clean (Task 6 retired the refusal) but produce nothing, because Task 7 built only vs_baseline. reference.md:2076 gives them their own contrasts: block with id, of, against and per-step metrics. Task 6 un-refused a thing Task 7 does not build: the silent-no-op class, created by this slice.
Task 7: fix round 1 at 08ba1eb (paired_percentile_of_derived takes compute_of and compute_against evaluated over the same drawn keys; results.contrasts built as a sibling of results.conditions per reference.md:2076, with of/against recorded via condition_dir_name; spec-defects entry removed). 673 tests. Verified on the worked-example shape: delta -0.00038 with ci95 [-0.00396, -0.00009], non-degenerate and bracketing, where the old shape gave [0.0000, 0.0000].
Task 8: implemented at 88bb6a0 — ZERO src/ changes; every interface was already reachable from main(['run']). 675 tests. Hand run over 120 units outside the repo: baseline pred ci95 width 12.577, spearman condition 12.574, paired vs_baseline delta width 0.181 — about 70x narrower, which is the allocation: within claim demonstrated rather than asserted.

Tasks 7+8: fix round at fba4040 — all four open findings (A, B, C, D) ADDRESSED. 679 tests, ruff/mypy clean.
  A: Comparison gains `declared: bool`; _baseline_comparisons/_declared_comparisons read it instead of reconstructing `against == baseline and id == label_of(of)`. Both docstrings rewritten — the argument against a fourth field was the thing that was wrong. Mutation-checked: restoring the identity test fails both new placement tests.
  B: three end-to-end tests through main(['run']). A derived contrast (step records identical `pred` under both conditions, only the aggregate FORMULA varies by analysis.method — the worked example's shape) asserts the interval brackets a delta of 19.5; passing compute_of for both sides gives 19.5 outside [0,0] and fails it. A declared statistics.contrasts entry shaped exactly like a generated one asserts placement in results.contrasts AND that vs_baseline still holds the unrestricted n_paired=10 block against the declared entry's within-restricted 5.
  C: `width < per_condition` -> `< per_condition / 10` (measured ratio ≈70, so a 7x margin).
  D: `hi > lo` -> the fixture's own arithmetic. The 120 per-unit differences are 60 at 1.5 and 60 at 0.5, so sd = 0.5·√(120/119), t(0.975,119) = 1.980100, width = 0.1815155 exactly — assertable because a t interval over a deterministic column draws nothing. CLAUDE.md's ≈0.033 (n≈228, linear-vs-rank) does not describe this fixture and the citation is gone.
  run_a_project gains `unit_attributes=` and the roster file always carries an alternating `cohort` column, since a `within` stratum needs the attribute declared in the config AND present in the table.
Task 6 minor CLOSED by B's declared-contrast fixture: its `of` names a string that is simultaneously a real condition label and the entry's own `id`, and the run validates clean — label-wins is now pinned by an assertion, not only by a docstring.

WHOLE-BRANCH REVIEW ran at fba4040 (opus subagent, independent) — .superpowers/sdd/2026-08-10-contrasts/whole-branch-review.md. Verdict CHANGES REQUIRED: 1 Critical, 3 Important, 4 Minor. Purity, paired_keys, cohens_dz, the one-draw-both-sides pairing, Comparison.declared, and absent-not-empty were all checked and found sound.
Whole-branch fix round at d082a36 — 691 tests, ruff/mypy clean.
  C1 (Critical, real, confirmed by me from the code): the DERIVED delta was of_summary - against_summary, the two conditions' WHOLE-SAMPLE aggregates, beside a ci95 built over base_keys. Under `within` or unequal completion those are different unit sets — a probe reported delta 509.5 with ci95 around 10.0, and an empty stratum reported a confident number at n_paired 0. Sibling of Task 7's Critical: that one fixed the interval and left the point estimate. Fixed structurally, per advisor: new pure stats.paired_delta_of_derived beside paired_percentile_of_derived, one call, same base_keys, None (never 0.0) on an empty intersection or a declining compute. Mutation-checked: restoring the aggregated difference fails both new end-to-end tests, and the pre-existing no-within derived test stays green at 19.5 as it must.
  C1 fixture note: the review's own probe had every cohort-a unit at pred 0.0, giving the legitimate point-mass ci95 [0,0] — too blunt. Ours varies within the stratum (pred = i, +1000 for cohort b), so both the stratum delta (10.0) and its interval are non-degenerate and `low < delta < high` is a real bracket.
  I2: E-STATS-CONTRAST-SAME-SIDES minted — reference.md:270 states the rule and named no code; of == against validated clean and published a perfect null with a zero-width interval. Checked only once both sides resolve so a typo stays E-STATS-CONTRAST-UNKNOWN (pinned by its own test).
  I3 + M7: E-STATS-CONTRAST-SHAPE minted for a non-list contrasts block, a non-mapping entry (`contrasts: [method=spearman]` crashed run with AttributeError), and a missing/non-string id (reached run.yaml as the string 'None'; was Task 1's deferred minor, closed here since it corrupts an artifact). contrasts.py's comment claiming validate guards this path is now true.
  I4: W-STATS-CONTRAST-THIN scoped to `within` contrasts per reference.md §Contrasts, §The one config file and §Validation. The old test asserted the substring 'min_reported_n', which both the where field and the message contain — the reviewer renamed the code and all 679 stayed green. Now asserts the literal identifier, plus a test that an unstratified thin contrast does NOT warn (min_reported_n: 10 ships in every generated config, so the old scope fired on any sub-ten-unit pilot).
  M5, M6: spec-defects entries — `confounded: true` assigned to S4c with the correction family (it needs the same which-axes-differ derivation as `paired`); max_ineligible_fraction reassigned S4b -> S4c and the S4a carry-table row updated to point at it.
  M8 (is_derived uses `or`): NOT changed, recorded instead. Flipping to `and` routes the same one-sided case into the column branch, where col_keys comes out empty and the entry is null by another route — no better, and no reachable test discriminates.
Docs touched: docs/superpowers/spec-defects.md only. Mechanical pass run over it (no trailing whitespace, no tabs, no invisible unicode, no duplicate anchors, table rows match their header).

RE-REVIEW at d082a36 (same reviewer, context intact): APPROVED FOR MERGE. C1 ADDRESSED with all three manifestations verified independently — including UNEQUAL COMPLETION, which no test covers: the reviewer built a step calling io.skip on half the roster under one condition only and measured delta 14.5 with ci95 [12.7, 16.3], where the old whole-sample 19.5 would have sat OUTSIDE the interval. So C1's second manifestation was live, not theoretical. The W-STATS-CONTRAST-THIN rename mutation that left 679 green now fails. I2, I3+M7, I4, M5, M6 all ADDRESSED (verified through main(['validate']), not only unit tests); M8's decline accepted. Sibling sweep found no other place a point estimate, interval, n, or threshold is computed over a different unit set than the number beside it.
Re-review's two NEW findings, closed at d9e3f7f — 693 tests, ruff/mypy clean:
  N9 (Important): W-STATS-FAMILY reported len(conditions) - 1 and never counted declared contrasts — accurate only while the block was refused wholesale, which this slice ended. A 2-condition run with two declared contrasts was told it published one comparison while publishing three. Now baseline + declared, pinned by a test asserting 'family of 3'. The baseline half ALSO overcounts a grid-only sweep (no sweep.baseline means no baseline comparisons at all, yet it reports len-1): pre-existing, NOT fixed, recorded in spec-defects for S4c because narrowing it removes the "correction is not implemented" disclosure this build still owes.
  N10 (Minor): duplicate contrast ids now refused under E-STATS-CONTRAST-SHAPE — the missing-id message already asserted two entries cannot share one.
Review of d9e3f7f: N9 and N10 ADDRESSED (all six conditions×declared shapes enumerated for the trigger change; all four seen_ids interactions probed), but found R11 (Important, REGRESSION, mine): N9's count called len() on the raw statistics.contrasts value in _check_sweep, which runs BEFORE _check_contrasts' shape refusal — so `contrasts: 5` raised TypeError out of validate, breaking the collects-never-raises constraint. It landed because the existing shape test used a mapping, on which len() works.
R11 fixed at f786b79 (isinstance guard; test covers 5, True and a bare string; mutation-checked against the pre-fix file). Also corrected the deferred-baseline-overcount reasoning per the reviewer: a grid-only sweep publishes NO comparison, so the W-STATS-FAMILY it emits is a false positive, not a disclosure that is owed — spec-defects now tells S4c to derive the count from resolve_contrasts and let the warning disappear. My earlier reasoning was wrong.
Reviewer's non-blocking note recorded in spec-defects: statistics.contrasts belongs in _check_shape's nested-shape pass, whose own comment describes R11's mechanism exactly ("the crash just moves one level down"). Both readers are guarded now; S4c's next reader of the block would otherwise inherit it.
CONFIRMED APPROVED at f786b79 — 694 tests, ruff, mypy green. Merged to main as a --no-ff merge commit, matching every prior slice (0fc2a6f, bcc8e90, 442810a, 99d6df7 are all slice merges).
Note for whoever reads this next: docs/superpowers/ and .superpowers/sdd/ are BOTH gitignored, so this ledger, the review files and spec-defects.md are on disk only and appear in no commit or diffstat.
Superseded note: d9e3f7f had NOT been re-reviewed at the time the line below was written. It is two validate-side changes plus one spec-defects entry, each with a test; the merge decision rests on the d082a36 approval.

=== HANDOFF (superseded by the fix rounds above; findings A-D and the whole-branch Critical are closed) ===
Branch s4b-contrasts, HEAD 88bb6a0, 675 tests / ruff / mypy all green, tree clean.
Tasks 1-6 complete and reviewed. Tasks 7 and 8 have OPEN findings, and the whole-branch review has NOT run.

OPEN — Task 7 review (spec ✅, 2 Important):
  A. cli.py:168/188/363 — 'auto-generated?' is reconstructed as (against == baseline.index and id == label_of(of)). validate._check_contrasts permits an id equal to a condition's own label, so such a declared contrast is misfiled into vs_baseline and, because declared entries come second and out[comp.of] = block overwrites, it REPLACES the genuine unrestricted baseline block (all-null if its within matches nobody) and never reaches results.contrasts. Data loss in the record. Fix: add declared: bool to Comparison in contrasts.py — the docstring argument against a fourth field is the thing that is wrong. THIS IS MY PLAN DEFECT (Task 1 argued against the field).
  B. tests/test_cli.py:771 — no test produces a DERIVED contrast through main(['run']), and none declares a statistics.contrasts entry end to end. Consequences: cohens_d is None for derived is code-verified only; passing compute_of twice positionally at cli.py:246-252 would reintroduce Critical 1 with all tests green (test_stats.py:1268 guards stats.py, not the call site); and all of Critical 2 can be reverted invisibly. run_a_project(aggregate_returns=...) is the lever.

OPEN — Task 8 review (mine, since the subagent limit was reached; spec ✅, 2 Important, BOTH from my brief):
  C. tests/test_cli.py — 'assert width < per_condition' passes at 12.5 against 12.577, where the hand run measured 0.181 against 12.577. Use a ratio as Task 5's unit test does (< per_condition / 4).
  D. tests/test_cli.py — 'assert hi > lo' is near-vacuous and does not test what its docstring claims (it cites ~0.033 as a floor). Note Task 5 established zero-width is LEGITIMATE for a point-mass bootstrap, so this cannot simply be strengthened to non-degenerate without qualification.

NOT RUN — the whole-branch review. It has found a Critical on every slice so far, including both of S4b's. It must run before merge, on the most capable model, over merge-base(main, HEAD)..HEAD.

Deferred minors already recorded above: Task 1's entry.get('id') coercion and by-reference within; Task 3's hash-seed-dependent sorted test; Task 6's unpinned label/id collision.
