# Task 2 report: the documents — three under-specifications, thirteen codes, two rows, and the inference-base ruling

**Status:** Complete. **Commits:** `3b5e942` — `docs: settle the holdout's three under-specifications, its thirteen codes, and the inference base`; `efba1e7` — closes the cross-document contradiction in `experimental-designs.md`; `641b63c` — closes two further, purely self-inflicted contradictions inside `reference.md` itself, found while re-checking after `efba1e7` (see **Concerns**, both added after review flagged the first).

**Files changed:** `docs/reference.md`, `docs/superpowers/spec-defects.md`, `docs/experimental-designs.md` (one sentence, added in `efba1e7` to close a contradiction — not part of the brief's Step 3 list). No `src/`, no test file.

## Summary

Six edits to `docs/reference.md` and one append to `docs/superpowers/spec-defects.md`, all taken verbatim from the brief's Step 3 text (no wording deviations found necessary):

1. **§ A fixed holdout split** — settled the `by_attribute` literals (`train`/`test` are fixed, not "whichever two values are there", refused as `E-DATA-HOLDOUT-VALUES`); replaced the fourth interaction bullet, which previously stated the split "happens within each cell" as a working feature, with the actual ruling — a roster-wide holdout or fold beside `allocation: between`/`sweep.groups` is **refused** (`E-DATA-HOLDOUT-CELLS`, `E-REPL-FOLD-CELLS`), drawing within cells being unbuilt and owned by H3c-3; and appended the inference-base paragraph (a holdout narrows a denominator, adds nothing to the correction family, training units count nowhere, `provenance.units.n`/`units_hash` stay whole-roster).
2. **§ Validation** — two new rows after *Holdout strata survive clustering*: "One split, not one cell each" and "Holdout leaves a test partition".
3. **§ Errors `validate` reports** — twelve rows inserted after `E-DATA-ASSIGN-VARIES` (last of the existing `E-DATA-ASSIGN-*` family), in the Global Constraints table's order: METHOD, FRAC, FROM, NO-DRAW, SEED, STRATIFY-UNKNOWN, FOLD, VALUES, STRATIFY-VARIES, EMPTY, CELLS, then `E-REPL-FOLD-CELLS`.
4. **§ Errors core raises** — the thirteenth code, `E-DATA-HOLDOUT-VARIES`, inserted as its own row immediately after the existing combined `E-DATA-CLUSTER-VARIES`/`E-DATA-WEIGHT-VARIES`/`E-DATA-ASSIGN-VARIES` row (not merged into it, since it is raised by `resolve_units` at run time only and has no `validate`-time counterpart, unlike its three siblings).
5. **`allocation.json`** — the JSON example's `"holdout"` line now carries `seed`/`strata` inside its own block; added the paragraph explaining why (a holdout is not an axis, so it gets a self-contained block rather than borrowing the axis-keyed `seed`/`strata` maps; `by_attribute` carries neither).
6. **§ What `auto` derives from** — added the `data.units.holdout.seed` row to the four-row table, and extended the "seed that is present must be one or the other" paragraph to name `E-DATA-HOLDOUT-SEED` and state the digest-exclusion reasoning already given for `assign.<axis>.seed`.
7. **§ Weighted samples** — one sentence added on what `resample` draws over under a holdout (the test partition alone).
8. **`docs/superpowers/spec-defects.md`** — appended the `technical_n` gap, filed with owner "whichever slice next changes `_cond_beside_n`, or H3c-3", not fixed.

## Verification

- Step 1/4 greps: all thirteen codes now print non-zero counts in `docs/reference.md`; `"holdout":` example line carries `seed`/`strata`; `holdout.seed` appears in a real table row (`§ What auto derives from`), not just prose.
- Mechanical pass: no trailing whitespace, no tabs, in either file. All new/existing anchors referenced (`#a-fixed-holdout-split`, `#errors-validate-reports`, `#clustered-units`, `#what-isnt-a-repeat`, `#validation`) resolve to existing headings — no heading was added, so no collision is possible. New table rows match their header's column count (§ Errors rows: 2; § Validation rows: 2; `auto`-derives table: 3). No `x` used for multiplication anywhere touched.
- Cross-document pass: `grep -rn "holdout" docs/design-principles.md README.md` — neither states a rule these edits contradict, and neither shows a holdout value as derived where these edits declare it settable or vice versa.
- The `NOT BUILT` marker on `holdout: null` (§ The one config file) and the "Three declarations above are not yet built" count are both untouched — verified by direct grep after all edits, not just by not having edited that region.
- `uv run pytest -q` → **1803 passed, 2 xfailed** (unchanged from before this task — a documents-only task adds no test). `tests/test_materialize.py` passed untouched. `uv run ruff check .` → All checks passed. `uv run mypy` → Success, 42 source files.
- Mutation (Step 5): temporarily renamed one `E-DATA-HOLDOUT-VALUES` occurrence to `E-DATA-HOLDOUT-VALUE`, re-ran the Step 1 grep loop — printed `E-DATA-HOLDOUT-VALUES 0`, confirming the loop can fail. Reverted; re-ran, confirmed both occurrences intact (count 2) and the loop passes.

## A plan defect worth stating plainly

Task 8's own brief (§ "Two false present-tense claims...") correctly attributes the § A fixed holdout split rewrite to task 2 and correctly names two of the four false claims this created: `experimental-designs.md`'s "folds and holdouts are drawn *within* each cell" and `reference.md` § Cross-validation's "Under `allocation: between`, folds are drawn within each cell" — both marked in task 8's brief as claims *it* will make honest when it lands. **Task 20's brief is the one that misattributes**, in its § Step 3(b) instruction for `experimental-designs.md` § Train-test holdout: *"the cells refusal strengthens that section rather than weakening it; say so if the section's wording implies folds and holdouts are drawn per cell (task 8 already rewrote the one paragraph that did — read it first rather than rewriting it twice)."* Read literally, this tells task 20's implementer the reconciliation is already done and to only *check*, not *fix* — but at the time task 20 runs, that reconciliation is task 8's to do, six tasks later, and the paragraph in question needed fixing well before then (see **Concerns**, below, for why it could not wait).

**Current wording, for whoever next touches any of these four spots** — all closed here, in task 2, not task 8:
- `reference.md` § A fixed holdout split's fourth interaction bullet: *"A roster-wide split beside a cell structure is refused, not drawn. … Drawing *within* each cell is the design that lifts both refusals, and it is not built."*
- `reference.md` § Validation's *Folds fit inside the cells* row: now reads *"Superseded by One split, not one cell each, the row for the identical combination: … refused outright (`E-REPL-FOLD-CELLS`) … since drawing folds within a cell is not built"* — left in place, not deleted, because `docs/superpowers/H3c-3-SCOPING.md` cites this row by name as the one it will eventually retire once folds-within-cells actually lands.
- `reference.md` § Cross-validation's opening bold claim: now *"Under `allocation: between`, a roster-wide fold is refused rather than drawn within each cell"*, with the "not an exception to once per run" sentence removed since it argued for a mechanism (per-cell drawing) that no longer exists in this text.
- `reference.md` § Repeat kinds' `fold` table row: now says the combination is "refused rather than drawn," linking to § A fixed holdout split, instead of asserting the draw as built.
- `experimental-designs.md` § Between-subjects factorial's "Every cell is a condition..." paragraph: now *"A fold or a holdout drawn within each cell is not built: … refused outright rather than drawn per cell — see reference.md § A fixed holdout split — so a design that needs one waits for that to land."*

Task 8 and task 20 can build on this wording rather than re-deriving it. Task 20 in particular should not skip `experimental-designs.md` § Between-subjects factorial believing task 8 already reconciled it — that specific paragraph is closed as of this task, not task 8.

## Concerns

- **This task's own first commit (`3b5e942`) introduced real contradictions — one cross-document, two more found on a second pass, all self-inflicted, all closed before this report was finalized.** My rewrite of `reference.md` § A fixed holdout split's fourth interaction bullet correctly states the cells combination is now *refused* and drawing within cells *is not built* — but three other passages, all consistent with the *old* text before my edit, kept asserting the opposite as present-tense fact: `experimental-designs.md`'s "folds and holdouts are drawn within each cell" (a cross-document contradiction, closed in `efba1e7`), and, found only on a second pass after that fix, two more **inside `reference.md` itself** — the § Validation row *Folds fit inside the cells* and § Cross-validation's own opening claim, plus a third instance in § Repeat kinds' `fold` table row (all three closed in `641b63c`). `CLAUDE.md` treats a contradiction "in a single section" as "a real inconsistency, not a wording nit" — these three are worse than the cross-document one in exactly that sense, since they sit inside the one document whose internal consistency was this task's most direct responsibility, and I did not check for them on the first pass. All four are now closed, minimally, in `reference.md`'s own established wording, without doing task 8's fuller job (no refusal-prose was sited anywhere new, no test was added, § Mistakes core prevents was not touched) or task 20's (the dated feasibility re-measurement, the honest-count writeup). Full suite (`1803 passed, 2 xfailed`), mechanical pass (no trailing whitespace/tabs, all anchors resolve, `×` preserved, no heading added), ruff and mypy clean, re-run after each of the three fix commits.
- **Lesson for whoever reviews a similarly-scoped documents task next:** rewriting one passage that states a design decision is not enough — grep the *same document* for other passages describing the same mechanism before treating the section as closed. I checked the two files the brief's own Step 4 named (`design-principles.md`, `README.md`) and the one file the plan explicitly flagged (`experimental-designs.md`) but did not, on the first pass, re-search `reference.md` itself for restatements of the exact mechanism I had just changed.
- Everything else in the brief was followed verbatim; no other conflicts between the brief and the current code/docs were found (task 1's brief had three; this task, being documents-only with no code to check the prose against, had none of that class).
